"""Material-aware gripper selection and minimum-force prediction.

Flat package: one module per concern. See docs/architecture and the repo
README for the data flow. All tuning lives in config.yaml, loaded via
`force_prediction.config.load_config`.
"""

__version__ = "0.1.0"
